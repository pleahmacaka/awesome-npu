---
import { products, renderProduct } from '../../../scripts/render.mjs';
export function getStaticPaths() {
  return products.map(p => ({ params: { slug: p.slug }, props: { html: renderProduct(p) } }));
}
const { html } = Astro.props;
---
<Fragment set:html={html} />
